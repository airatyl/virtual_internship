Ваша задача: заполнить в ответе свойство agreementPrice согласно
следующей формуле:

    agreementPrice = days between agreementDateFrom and agreementDateTo

PS: на данный момент это фейковое значение.
Реализация реальных бизнес требований начнётся в районе 30 шага.

PS: загуглите как рассчитать количество дней между двумя датами
в Java. Для этого есть стандартное решение, найдите его и
примените в своём проекте. Хороший программист должен научиться
чувствовать, когда надо самому писать новый код, а когда можно
найти готовое решение стандартной задачи в интернете.

Не переживайте, если первые задания кажутся вам очень простыми.
Мы хотим, чтобы вы постепенно вкатились в проект, привыкли к процессу
разработки, наладили общение с ментором и коллегами. Задания постепенно
будут становиться всё сложнее и сложнее.

PS: не забудьте протестировать ваши изменения.
Если это необходимо, внесите изменения в уже написанные
ранее тесты для того, чтобы они по-прежнему работали.

PS: перед комитом в репозиторий обязательно прогоняйте все
ваши тесты. Все тесты должны проходить без ошибок (быть зелёными).
Если тесты поломались, их надо починить перед комитом, а не после него.

PS: быстро найти все изменения сделанные в текущем задании
можно в IDEA (закладка слева) выбрав папку текущего задания (например task_10)
и папку предыдущего задания (например task_9) и в сплывающем меню выбрав пункт
Compare Directories (Ctrl + D).

PS: не забудьте по окончанию выполнения шага создать и заполнить
файл /documentation/Task_x_description.txt. Формат заполнения этого
файла описан в task_1 в файле Learning_reflection.txt.

**Контекст задачи (краткое описание) **
TODO: опишите контекст и задачу своими словами в свободной форме.

необходимо было добавить вычисление   agreementPrice = days between agreementDateFrom and agreementDateTo



**Что было сделано и как? (краткое описание)**
TODO: опишите что и как делали для того, чтобы выполнить задачу.
для вычисления количество дней между Date я вычел время в миллисекундах из даты прибытия дату отправления и перевел их в дни и округлил до целого значения
 для этого я   response.setAgreementPrice(new BigDecimal((int)((response.getAgreementDateTo().getTime() -response.getAgreementDateFrom().getTime())/(1000*60*60*24))-1));
 и исправил заполнение полей ответа
 было
return new TravelCalculatePremiumResponse(
                request.getPersonFirstName(),
                request.getPersonLastName(),
                request.getAgreementDateFrom(),
                request.getAgreementDateTo());
стало
        TravelCalculatePremiumResponse response = new TravelCalculatePremiumResponse(
                request.getPersonFirstName(),
                request.getPersonLastName(),
                request.getAgreementDateFrom(),
                request.getAgreementDateTo());
                response.setAgreementPrice(new BigDecimal((int)((response.getAgreementDateTo().getTime() -response.getAgreementDateFrom().getTime())/(1000*60*60*24))-1));
                return response;

    }
**Зачем это было сделано? Чему научился в процессе выполнения задания?**
TODO: опиши своими словами зачем было необходимо выполнить это задание,
опиши какие техники, технологии, инструменты, использовал для выполнения задания,
чему научился выполняя это задание?


**Сколько потратил времени на выполнение задачи?**
TODO: зафиксируй для себя сколько времени потратил на выполнение этого задания. Можно ли было его сделать быстрее?


**Заметки, идеи, предложения в свободной форме**
TODO: зафиксируй любые свои мысли, эмоции, идеи, размышления связанные с этим заданием.